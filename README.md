# Notas by Nico

PWA de tareas con checklists, prioridades, etiquetas, recordatorios y subtareas.

## Deploy

Subido a Vercel vía GitHub. No requiere build — son archivos estáticos puros.

## Uso local

```bash
npx serve .
```

Abrí `http://localhost:3000` en el navegador.
